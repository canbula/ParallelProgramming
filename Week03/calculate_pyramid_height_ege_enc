def calculate_pyramid_height(number_of_blocks):
    n = 1
    height = 0
    if number_of_blocks == 1 or number_of_blocks == 2:
        height = 1
        return height
    
    while(number_of_blocks > n):
        height += 1
        number_of_blocks -= n
        n += 1

    return height
